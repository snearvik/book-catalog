{% extends 'base.html.twig' %}

{% block body %}
	<div class="container no-gutters" style="margin-bottom: 15px">
		<div class="row justify-content-start align-items-center">
			<div class="col-md-auto">
				<h1>{{ book.name }}</h1>
			</div>
			<div class="col-md-auto">
				<a class="btn btn-primary" href="{{ path('book_edit', { 'id': book.id }) }}" role="button">Редактировать</a> 
			</div>
		  </div>
	</div>    
	<div class="container no-gutters">
		<div class="row justify-content-start align-items-top">
			<div class="col-md-auto">
				<img src="{{ asset('uploads/covers/' ~ book.cover) }}" alt="{{ book.cover }}" />
			</div>
			<div class="col-md-auto">
				<table class="table">
					<tbody>
						<tr>
							<th>Год издания</th>
							<td>{{ book.publishingYear }}</td>
						</tr>
						<tr>
							<th>ISBN</th>
							<td>{{ book.isbn }}</td>
						</tr>
						<tr>
							<th>Количество страниц</th>
							<td>{{ book.pageCount }}</td>
						</tr>
						<tr>
							<th>Авторы</th>
							<td>
								{% for author in book.authors %}
								{{ (author.getFullName()) }}<br>
								{% endfor %}
							</td>
						</tr>
					</tbody>
				</table>					
			</div>			
		</div>
	</div>
	
{% endblock %}
